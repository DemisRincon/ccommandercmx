import React from 'react'
import {WrapperGrid} from '../global'
import {CardContainer, CardImage} from '../card'
const ImageGridElement = (prosp) => {
    console.log(prosp)
  return (
    <WrapperGrid>
        hello
    </WrapperGrid>
  )
}

export default ImageGridElement